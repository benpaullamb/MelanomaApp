const tf = require('@tensorflow/tfjs');
const tfn = require('@tensorflow/tfjs-node');

// Accuracy, loss
// Error

// Confusion matrix
// TP, TN, FP, FN

// Sensitivity = (True +) / (Class +)
// Specificity = (True -) / (Class -)

// Precision = (True +) / ((True +) + (False +))
// Recall = (True +) / ((True +) + (False -))

// Receiver Operating Characteristic curve – sensitivity / specificity trade-off
// Precision Recall curve – precision / recall trade-off
